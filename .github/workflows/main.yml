on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'before'
        run: ls

      - uses: actions/checkout@v3

      - name: 'login to Docker hub '
        run: docker login --username zoz312 --password dckr_pat_Fhh-nnut_KnDHTfV3XgdsGfX8WQ

